[project]
id = "citizen-landing"
name = "Citizen Landing Page"
version = "1.0.0"

[dokku]
port = 3000

[deploy]
health_check = "/api/health"

[build.env]
NODE_ENV = "production"
NEXT_TELEMETRY_DISABLED = "1"
